import { RouteLoadingFallback } from "@/components/layout/route-loading-fallback";

export default function InspectionsLoading() {
  return <RouteLoadingFallback label="Loading site inspections…" />;
}
