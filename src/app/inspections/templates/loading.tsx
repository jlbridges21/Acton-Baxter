import { RouteLoadingFallback } from "@/components/layout/route-loading-fallback";

export default function InspectionTemplatesLoading() {
  return <RouteLoadingFallback label="Loading templates…" />;
}
