import { RouteLoadingFallback } from "@/components/layout/route-loading-fallback";

export default function InspectionTemplateEditorLoading() {
  return <RouteLoadingFallback label="Loading template editor…" />;
}
