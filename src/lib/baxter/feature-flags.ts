/**
 * UI feature flags for Baxter admin surfaces.
 * Keep backend/job paths intact when hiding — re-enable by flipping the flag.
 */

/** When false, Process Monitoring is hidden from nav and admin links (route/API still exist). */
export const PROCESS_MONITORING_UI_ENABLED = false;
